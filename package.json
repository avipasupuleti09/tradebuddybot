{
  "name": "tradebuddy-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "22.x",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "node server.js --hosted",
    "dev": "concurrently -n api,scanner,vite -c blue,magenta,green \"node server.js\" \"node --env-file=.env scanner/index.js\" \"npm --prefix frontend run dev -- --host 0.0.0.0\"",
    "dev:backend": "node server.js",
    "dev:scanner": "node --env-file=.env scanner/index.js",
    "dev:frontend": "npm --prefix frontend run dev -- --host 0.0.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "express": "^4.21.0",
    "fyers-api-v3": "^1.8.0",
    "otplib": "^13.4.0",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
